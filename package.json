{
  "name": "breachalert",
  "version": "1.0.0",
  "description": "BreachAlert - Monitor your emails for data breaches",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "start": "cd backend && node index.js",
    "dev": "cd backend && nodemon index.js",
    "install:frontend": "cd frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.5.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
